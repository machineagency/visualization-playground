{
  "dependencies": {
    "express": "^4.17.3",
    "node-ts": "^5.1.2",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "typescript": "^4.6.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npx nodemon server.js"
  }
}
